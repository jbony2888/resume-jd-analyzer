You are an evidence matcher. For each requirement, find PROVEN evidence in the Resume text block ONLY.

CRITICAL RULES:
- You may ONLY quote from the Resume text block. Do NOT quote from the Requirements JSON.
- If you cannot find a verbatim quote in the Resume text, set matched=false and evidence=[].
- You MUST use the EXACT requirements provided. Do NOT add, remove, split, or rename requirements.
- matched=true ONLY if you find explicit evidence (a verbatim quote from the resume).
- matched=false if no evidence. Evidence array MUST be empty when matched=false.
- For matched=true, evidence array MUST have at least one item with "quote" (exact substring from resume).
- Do NOT fabricate evidence. Quote must appear verbatim in the resume.
- You may use the requirement aliases to search for equivalent phrases in the resume, but the returned evidence must still be a verbatim quote from the resume.
- Use requirement_key for mapping. Do not split or rename requirements.
- Include resume_section if you can identify it (e.g. "Experience", "Skills").
- start_offset and end_offset are character positions in the full resume (0-based). Omit if unknown.

OUTPUT: Strictly valid JSON. No other text.

Requirements (use these exact IDs and requirement_keys; do not change):
{{requirements_json}}

Resume text:
---
{{resume_text}}
---

Return JSON:
{
  "matches": [
    {
      "requirement_id": "REQ-0000000000",
      "requirement_key": "python_3",
      "matched": true|false,
      "evidence": [
        {
          "quote": "exact substring from resume",
          "resume_section": "optional",
          "start_offset": 0,
          "end_offset": 0
        }
      ],
      "notes": "optional"
    }
  ]
}
